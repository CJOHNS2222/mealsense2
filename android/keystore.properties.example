# Copy this file to android/keystore.properties and fill in your values.
# Path is relative to android/ (use ../ if your keystore is in android/)
# Example creates a keystore at android/mealsense-release.jks

storeFile=mealsense-release.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=mealsense
keyPassword=REPLACE_WITH_KEY_PASSWORD
